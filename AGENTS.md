# 중요 지침

## 코드 작성 원칙

### LLM이 이해하고 관리하기 쉬운 코드
- 함수와 파일은 작게 유지하고, 하나의 책임만 갖게 한다.
- 이름은 의도가 드러나게 짓는다 (축약어 지양).
- 암묵적 동작이나 과도한 추상화보다 명시적이고 단순한 코드를 선호한다.
- 복잡한 로직에는 "왜"를 설명하는 짧은 주석을 단다.
- 기존 코드의 패턴과 구조를 따르고, 불필요한 새 의존성을 추가하지 않는다.

### 필요 최소한의 단위 테스트
- 핵심 비즈니스 로직과 버그가 나기 쉬운 부분만 테스트한다.
- 단순 getter/setter, UI 배치, 외부 라이브러리 동작은 테스트하지 않는다.
- 테스트는 짧고 읽기 쉽게, 하나의 테스트는 하나의 동작만 검증한다.

### 사용자 친화적인 UX 우선
- 기술적 편의보다 사용자 경험을 먼저 고려한다.
- 오류 메시지는 무엇이 잘못됐고 어떻게 해결하는지 쉬운 말로 알려준다.
- 로딩, 빈 상태, 오류 상태를 항상 처리한다.
- 사용자의 입력과 작업이 유실되지 않도록 한다.  