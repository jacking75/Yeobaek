<Window x:Class="Yeobaek.Ui.ArchiveWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="보관함 - 여백" Width="880" Height="620" MinWidth="560" MinHeight="400"
        Icon="../Assets/yeobaek.ico"
        FontFamily="{StaticResource UiFont}" FontSize="13"
        Background="{StaticResource SurfaceBrush}" Foreground="{StaticResource TextBrush}"
        WindowStartupLocation="CenterScreen">
  <DockPanel Margin="20">
    <DockPanel DockPanel.Dock="Top" Margin="0,0,0,12">
      <TextBlock DockPanel.Dock="Left" Text="보관함" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,16,0" />
      <TextBox x:Name="SearchBox" Style="{StaticResource FieldTextBox}" Tag="제목·본문 검색 (띄어 쓴 낱말은 모두 들어간 글만)"
               AutomationProperties.Name="보관함 검색" TextChanged="SearchBox_TextChanged" KeyDown="SearchBox_KeyDown" />
    </DockPanel>

    <DockPanel DockPanel.Dock="Bottom" Margin="0,12,0,0">
      <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
        <Button Style="{StaticResource PrimaryButton}" Content="보관본 열기" Click="Open_Click" />
        <Button Style="{StaticResource TextButton}" Content="원문 열기" Margin="8,0,0,0" Click="OpenSource_Click" />
        <Button Style="{StaticResource TextButton}" Content="삭제" Margin="8,0,0,0" Click="Delete_Click" />
      </StackPanel>
      <TextBlock x:Name="StatusText" Foreground="{StaticResource MutedTextBrush}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
    </DockPanel>

    <Grid>
      <ListView x:Name="ResultList" Style="{StaticResource PlainListView}" SelectionMode="Extended"
                HorizontalContentAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                MouseDoubleClick="ResultList_MouseDoubleClick" KeyDown="ResultList_KeyDown">
        <ListView.ItemContainerStyle>
          <Style TargetType="ListViewItem">
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
          </Style>
        </ListView.ItemContainerStyle>
        <ListView.ItemTemplate>
          <DataTemplate>
            <DockPanel>
              <TextBlock DockPanel.Dock="Right" Foreground="{StaticResource MutedTextBrush}" FontSize="12" Margin="12,0,0,0" VerticalAlignment="Top">
                <Run Text="{Binding SavedAt, StringFormat=yyyy-MM-dd, Mode=OneWay}" /><Run Text=" · " /><Run Text="{Binding SizeLabel, Mode=OneWay}" />
              </TextBlock>
              <StackPanel>
                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                <TextBlock Text="{Binding Site}" Foreground="{StaticResource AccentBrush}" FontSize="12" Margin="0,1,0,0" />
                <TextBlock Text="{Binding Snippet}" Foreground="{StaticResource MutedTextBrush}" FontSize="12" Margin="0,2,0,0"
                           TextTrimming="CharacterEllipsis" />
              </StackPanel>
            </DockPanel>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
      <TextBlock x:Name="EmptyText" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center"
                 TextAlignment="Center" TextWrapping="Wrap" MaxWidth="420" Foreground="{StaticResource MutedTextBrush}" />
    </Grid>
  </DockPanel>
</Window>
