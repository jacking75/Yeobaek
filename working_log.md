# 작업 로그

## 2026-09-23 14:24 KST — README 초보자용 사용 안내 추가, main 푸시

- README 에 "처음 사용하는 분을 위한 안내"를 추가했다: 설치·빌드·실행, 화면 구성, 링크 여는 법, 광고 숨기기(선택기·예외 사이트·차단 목록), 리더 모드, 나중에 읽기, 보관·검색, settings.json 설정, 문제 해결 표.
- 빌드 명령의 저장소 주소를 실제 주소(github.com/jacking75/Yeobaek)로 바꿨다.
- 지금까지의 구현 전체를 커밋해 main 에 푸시했다.

## 2026-09-23 14:13 KST — 오프라인 보관 + FTS5 전문 검색 (RSS 는 로드맵에서 제외)

- `Ctrl+S` 로 글을 보관한다. 본문을 추출하고 이미지를 내려받아 `archive\<폴더>\` 에 저장하며, 가상 호스트로 열어 인터넷 없이도 읽힌다(네트워크를 오프라인으로 강제하고 확인).
- 보관함 창(`Ctrl+Shift+F`)에서 제목·본문을 검색한다. FTS5 `trigram` 색인 + LIKE 로 2글자 한국어·단어 중간 일치를 지원하고, 보관본 열기·원문 열기·삭제(확인 후 파일·색인까지)를 제공한다.
- 티스토리 지연 로딩 이미지(`data-src`)가 리더 모드·보관본에서 빈 이미지가 되던 문제를 고쳤다(`LazyImages`).
- DB 스키마 3 으로 올림(articles, articles_fts). 테스트 5건 추가, 총 49건 통과.

## 2026-09-23 13:43 KST — 티스토리 사이드바 광고(쿠팡 파트너스) 차단

- 사용자가 준 티스토리 글에서 오른쪽 사이드바·본문 위아래 광고가 쿠팡 파트너스 위젯(`ads-partners.coupang.com`)임을 확인하고 기본 차단 목록에 넣었다(네이버 광고 라이브러리 등도 추가).
- 기본 차단 목록을 앱에 내장(`BlockRules.BuiltIn`)하고 blocklist.txt 는 더할 항목·뺄 항목(`!`)만 담도록 바꿨다. 기존 사용자 파일에도 새 기본 항목이 적용된다.
- DB 기본 숨김 규칙을 버전별 묶음으로 바꿔 업데이트 때 새 묶음만 심게 했다(사용자가 지운 규칙은 유지). 업그레이드·`!` 제외 테스트 추가, 44건 통과.
- `fundingchoicesmessages.google.com` 을 막으면 "광고 차단 소프트웨어" 경고가 뜨는 것을 실제 페이지에서 확인해 기본 목록에서 뺐다.

## 2026-09-23 13:28 KST — Spec.md 기반 여백(Yeobaek) 초기 구현

- .NET 10 WPF + WebView2 앱과 xUnit 테스트 프로젝트를 만들었다. 빌드 산출물은 루트 `bin\<구성>\`(TFM 폴더 없음), 테스트는 `bin\<구성>\tests\`.
- 1층 네트워크 차단(하위 도메인·경로 힌트, 워커 요청 포함), 2층 문서 생성 전 주입(생성형 스타일시트·AdSense 선점·링크 브리지·요소 선택기), 3층 리더 모드(렌더링된 DOM 추출 → 가상 호스트), 탭·팝업·우클릭 메뉴, 예외 사이트, 나중에 읽기, 규칙 관리 창을 구현했다.
- Spec.md 의 동작하지 않거나 위험한 부분(시각 트리 밖 WebView2 초기화, NewWindow 호출 순서, JSON 대소문자, documentElement null, 메뉴 항목 한도 등)을 고치고 `docs/spec-deviations.md` 에 이유를 정리했다.
- 실제 앱을 띄워 CDP·UI 자동화로 차단(204)·주입·Ctrl/가운데 클릭·팝업 차단·선택기 저장·리더 모드·예외 사이트 토글을 확인했고, 이 과정에서 찾은 규칙 갱신 경쟁 조건·주소창 표시 문제를 고쳤다. 빌드 경고/오류 0, 테스트 42건 통과.
